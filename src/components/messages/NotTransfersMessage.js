import React from 'react';

import CenteredMessage from 'src/components/messages/CenteredMessage';

const NoTransfersMessage = () => (
  <CenteredMessage title="No transfers found!" message="Register some, or select another date range." />
);

export default NoTransfersMessage;
